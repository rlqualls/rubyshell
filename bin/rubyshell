#!/usr/bin/env ruby

require File.dirname(__FILE__) + '/../lib/rubyshell'
require File.dirname(__FILE__) + '/../lib/rubyshell/shell'

shell = RubyShell::Shell.new

if ARGV.size > 0
	shell.execute ARGV.join(' ')
else
	shell.run
end

